# Docling Chat API

Этот проект предоставляет простой FAST API для обработки и поиска содержимого
документов Microsoft Word с использованием библиотеки **docling** в сочетании с
векторным поиском Milvus и внедрениями Яндекса.

---

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
    # create and activate a virtual environment (Windows)
    cd src
    py -m venv .venv
    .\.venv\Scripts\activate
    
    # install Poetry
    pip install poetry
    
    # install project dependencies
    poetry install
   ```

### 2. Настройте переменные окружения:

Скопируйте файл `.env.example` в `.env` и заполните необходимые значения, особенно `LLM_API_KEY`, `LLM_MODEL_NAME`
и `LLM_CATALOG_ID_YANDEX`

   ```bash
   cp .env.example .env
   ```

### 3. Соберите и запустите с помощью Docker Compose:

   ```bash
   docker-compose up --build
   ```

Когда сервисы запущены, вы можете получить доступ к API по ссылке http://localhost:18000/docs#/.

## API эндпоинты

Backend предоставляет HTTP-эндпоинты для индексации документов в Milvus и поиска похожих фрагментов текста (чанков),
извлечённых с помощью Docling.

Чанкование выполняется гибридным HybridChunker из Docling.
Для оценки размера чанков используется токенизатор Яндекса с тайм‑аутами.

Документы для индексации помещаются в папку uploads/.
При вызове index каждый .docx-файл обрабатывается, разбивается на чанки, и сохраняется в Milvus.

### POST /api/v1/doc/index

Пересоздаёт коллекцию Milvus (drop + create), затем индексирует все .docx файлы из папки uploads/.
Важно: документ(ы) не хранятся в памяти заранее — обработка выполняется только во время вызова этого эндпоинта.

**Request body**
Пустой объект.
Эндпоинт не принимает дополнительных параметров.

**Response body (application/json)**
Поле        Тип         Описание
inserted    integer     Количество успешно проиндексированных чанков.

### POST /api/v1/doc/similar

Выполняет поиск похожих чанков в Milvus, используя Yandex Embeddings.
Находит наиболее релевантные фрагменты текста (top_k лучших результатов).

**Request body (application/json)**
Поле    Тип         Описание
query   string      Пользовательский текстовый запрос.
top_k   integer     Количество результатов (по умолчанию: 5).

**Response body (application/json)**
Поле        Тип                 Описание
results     array of objects    Список найденных похожих чанков.

Каждый объект внутри results имеет структуру:
Поле        Тип             Описание
id          number          Внутренний ID элемента в Milvus.
distance    float           Мера близости (чем меньше — тем ближе).
text        string          Текст чанка.
metadata    object / null   Метаданные: headings + document_name + chunk_index.

Пример метаданных:
"metadata": {
"headings": ["2 ", "2.4 ", "2.4.1 "],
"document_name": "WKR.docx"
}

### POST /api/v1/doc/chunks-by-heading

Возвращает последовательность чанков выбранного документа, находящихся на том же уровне оглавления, что и указанный чанк.

**Request body (application/json)**

Поле            Тип      Описание
document_name   string   Имя документа (как при индексации).
chunk_index     integer  Индекс целевого чанка.
depth           integer  Глубина уровня headings: 1 — подраздел (последний элемент), 2 — родительский раздел и т.д.

**Ответ**

Поле             Тип                Описание
depth_used       integer            Какой уровень headings фактически применён (если запрошенный больше длины списка).
target_heading   string / null      Значение headings, по которому фильтровались результаты.
results          array of objects   Чанки с тем же разделом, отсортированные по chunk_index.
message          string / null      Сообщение о пустом оглавлении (если headings нет у целевого чанка).


### GET /health

Возвращает общее состояние приложения: есть ли связь с Milvus и готов ли загрузчик документов.

**Response body**
Поле        Тип         Описание
status      string      "ok" или "degraded".
milvus_ok   bool        Есть ли успешное соединение с Milvus.
doc_loaded  bool        Флаг, проверяющий есть ли строки в коллекции